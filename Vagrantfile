Vagrant.configure("2") do |config|
  config.vm.box = "bento/ubuntu-24.04"

  config.vm.hostname = "ubuntu-nginx-mod-security"

  config.vm.provider "virtualbox" do |vb|
    vb.name = "ubuntu-nginx-mod-security"
    vb.memory = "4096"
    vb.cpus = 2
  end
end

